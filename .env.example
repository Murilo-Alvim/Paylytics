# PostgreSQL connection string (Neon recommended)
DATABASE_URL="postgresql://user:password@ep-xxxx.region.aws.neon.tech/paylytics?sslmode=require"

# JWT secret for the mocked auth flow
JWT_SECRET="replace-with-a-long-random-string"

# Public app URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
